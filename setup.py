from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This repo will be used for end to end mlops cycle, from experiment tracking, data pipeline to CI/CD, docker and aws service.',
    author='Akshat Sharma',
    license='MIT',
)
